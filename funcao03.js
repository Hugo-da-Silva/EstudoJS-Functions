let V = function(x) { // variavel V recebe = função com parâmetro
    return x*2        // pedindo para retornar o valor de x multiplicado por 2 
}

let res = (V(5))
console.log(`O valor mutiplicado é igual a ${res}`)
// neste caso passamos para uma variavel um parâmetro e ele conseguiu executar uma função
